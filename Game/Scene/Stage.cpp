#include "Stage.h"

namespace Game::Scene {
	Stage::Stage(const std::string& name, Engine::Core::Context& ctx, Engine::Scene::SceneManager& s_m)
		:Scene(name, ctx, s_m)
	{

	}
	void Stage::Update(float)
	{
	}
	void Stage::Render()
	{
	}
}